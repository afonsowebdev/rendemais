{
  "name": "rende-mais",
  "version": "1.0.0",
  "private": true,
  "description": "Rende+ — finanças pessoais (build com esbuild + empacotamento nativo via Capacitor)",
  "scripts": {
    "build": "node build.mjs",
    "sync": "npm run build && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "ios": "npm run sync && npx cap open ios"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@capacitor/cli": "^8.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  }
}
